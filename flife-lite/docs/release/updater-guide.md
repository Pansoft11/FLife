# Updater Guide

RC1 includes `update-manifest.json` as a signing-ready placeholder.

Before enabling production updates:

- replace placeholder URL
- sign update artifacts
- publish signature manifest
- test rollback path
