# Known Limitations

- Native Windows packaging requires Rust, Cargo, MSVC Build Tools, and Windows SDK.
- Plotly remains a large lazy-loaded results chunk.
- PDF export is represented by HTML/DOCX-ready report output pending final PDF renderer integration.
- FEA importers are parser foundations, not full vendor-native readers.
- AI hooks are interfaces only.
